#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#
# ---
source $HOME/.owl4ce_var

# there was once a pulseaudio here

# UI Appearance
bash -c "$VISMOD_DIR/mode-toggle startup"

# Compositor
picom -b

# Authentication Agent - PolicyKit
lxpolkit &

# Power Manager
[[ -f "`which xfce4-power-manager`" ]] && xfce4-power-manager &

# Sessions Autolock
[[ -f "`which xautolock`" ]] && xautolock -time $AUTOLOCK_MINUTE -locker "$($DEFAPPS_EXEC -g lockscreen)" -detectsleep &

# Others
# Below will autostart mpd
[[ -f "`which mpd`" ]] && bash -c "$MPD_TCD"
